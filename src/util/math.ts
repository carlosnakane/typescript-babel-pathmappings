const sum = (firstNumber: number, secondNumber: number) => firstNumber + secondNumber;
const multiply = (firstNumber: number, secondNumber: number) => firstNumber * secondNumber;

export { sum, multiply };
